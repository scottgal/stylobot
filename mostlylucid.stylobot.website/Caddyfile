# ============================================================
# Caddyfile for Stylobot
# Automatic HTTPS via Let's Encrypt for stylobot.net
# Fallback routing for any other host (local dev, testing, etc)
# ============================================================
#
# Architecture:
#   Internet → Caddy (TLS) → YARP Gateway (bot detection) → Website
#                           → Website (direct for SignalR hub)
#
# CSP Note:
#   The site uses: Alpine.js, DaisyUI/Tailwind, boxicons (unpkg),
#   Google Fonts (Raleway), SignalR (cdn.jsdelivr.net),
#   Umami analytics, Web3Forms contact API.
#   Alpine's :style bindings require 'unsafe-inline' for styles.
#   The theme-switcher inline <script> requires 'unsafe-inline' for scripts.
# ============================================================

{
	email scott.galloway+sb@gmail.com
}

# Define shared routing logic for all hosts
(common_routes) {
	# Dashboard: route /_stylobot* directly to website container
	# (SignalR WebSocket upgrades fail through YARP, and
	# dashboard API/UI must bypass the gateway proxy)
	handle /_stylobot* {
		reverse_proxy website:8080 {
			header_up X-Real-IP {remote_host}
			header_up X-Forwarded-For {remote_host}
			header_up X-Forwarded-Proto {scheme}
			header_up X-Forwarded-Host {host}
		}
	}

	# Everything else → YARP gateway (bot detection layer)
	handle {
		reverse_proxy gateway:8080 {
			health_uri /health
			health_interval 10s
			health_timeout 5s

			header_up X-Real-IP {remote_host}
			header_up X-Forwarded-For {remote_host}
			header_up X-Forwarded-Proto {scheme}
			header_up X-Forwarded-Host {host}
		}
	}

	# Security Headers
	header {
		# HSTS (1 year, subdomains, preload-ready)
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

		# Prevent MIME type sniffing
		X-Content-Type-Options "nosniff"

		# Clickjacking protection
		X-Frame-Options "SAMEORIGIN"

		# Referrer policy
		Referrer-Policy "strict-origin-when-cross-origin"

		# Permissions policy (disable unused browser features)
		Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()"

		# Content Security Policy
		# 'self'                    - local scripts/styles/fonts/images
		# 'unsafe-inline'           - Alpine.js :style bindings + theme <script>
		# 'unsafe-eval'             - Alpine.js uses new Function() for expressions
		# cdn.jsdelivr.net          - SignalR client library + DaisyUI CSS
		# cdn.tailwindcss.com       - Tailwind CSS CDN (dashboard)
		# unpkg.com                 - boxicons (CSS + fonts)
		# fonts.googleapis.com      - Google Fonts CSS (Raleway)
		# fonts.gstatic.com         - Google Fonts WOFF2 files
		# umami.mostlylucid.net     - Umami analytics script + beacons
		# api.web3forms.com         - Contact form submission API
		# wss: ws:                  - SignalR WebSocket connections
		# data: blob:               - inline SVGs, canvas blobs
		Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdn.tailwindcss.com https://unpkg.com https://umami.mostlylucid.net; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com https://unpkg.com; font-src 'self' https://fonts.gstatic.com https://unpkg.com; img-src 'self' data: blob: https:; connect-src 'self' wss: ws: https://api.web3forms.com https://umami.mostlylucid.net; frame-ancestors 'self'; base-uri 'self'; form-action 'self' https://api.web3forms.com"

		# Remove server identity header
		-Server
	}

	# Compression
	encode gzip zstd

	# Access log
	log {
		output file /var/log/caddy/access.log
		format json
	}
}

# Production domains with HTTPS via Let's Encrypt
stylobot.net, www.stylobot.net {
	import common_routes
}

# Wildcard matcher for any other hostname (localhost, IPs, internal domains, etc)
# Caddy will handle TLS appropriately based on the host
:443, :80 {
	import common_routes
}
