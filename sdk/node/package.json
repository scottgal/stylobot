{
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/node",
    "packages/elements"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist"
  }
}
