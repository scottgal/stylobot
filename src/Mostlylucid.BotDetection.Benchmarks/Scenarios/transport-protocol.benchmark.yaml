name: TransportProtocol_Document
description: Transport protocol classification for document request
detector: TransportProtocol
request:
  method: GET
  path: /products/123
  ip: 203.0.113.42
  headers:
    user-agent: "Mozilla/5.0 Chrome/120.0"
    accept: "text/html"
    sec-fetch-mode: navigate
    sec-fetch-dest: document
thresholds:
  max_mean_ns: 3000
  max_allocated_bytes: 512
tags: [fast-path, transport, human]
