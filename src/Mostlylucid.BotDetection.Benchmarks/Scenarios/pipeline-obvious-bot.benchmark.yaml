name: Pipeline_ObviousBot
description: Full pipeline with obvious curl bot
detector: _pipeline
request:
  method: GET
  path: /api/data
  ip: 198.51.100.10
  headers:
    user-agent: "curl/8.4.0"
    accept: "*/*"
thresholds:
  max_mean_ns: 500000
tags: [pipeline, bot]
