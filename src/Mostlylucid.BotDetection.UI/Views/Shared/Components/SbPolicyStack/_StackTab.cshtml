@using Mostlylucid.BotDetection.UI.Models
@using Mostlylucid.BotDetection.UI.Services
@inject IDashboardLinkResolver DashboardLinks
@model PolicyStackViewModel
@if (Model.StackGroups.Count == 0)
{
    @* C12 -- zero-rules empty state mirrors the Effective tab so an
       operator landing on either tab sees the same Browse-templates /
       Start-blank pair when nothing is authored at this scope. *@
    <div class="sb-policy-empty" data-policy-stack-empty="true">
        <p>No manually authored rules yet at this scope.</p>
        <div class="sb-policy-empty-actions">
            <button type="button"
                    class="sb-template-use"
                    data-action="switch-tab"
                    data-target="templates">Browse templates</button>
            <a class="sb-template-preview"
               href="@DashboardLinks.Resolve($"/policystack/edit/new?scope={PolicyScopeUrl.Encode(Model.Scope)}")">Start blank</a>
        </div>
    </div>
}
else
{
    <div class="sb-policy-stack-stack-tab" data-policy-stack-tab="stack">
        @foreach (var group in Model.StackGroups)
        {
            @await Html.PartialAsync("Components/SbPolicyStack/_StackScopeGroup", group)
        }
    </div>
}
