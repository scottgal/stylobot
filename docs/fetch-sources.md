# External Fetch Sources

Generated by `stylobot --output-fetch-sources-doc docs/fetch-sources.md`, straight from `IFetchSourceRegistry.GetDeclarations()`.
Do not hand-edit — regenerate this file instead, or it drifts from what the binary actually does.

Environment at generation time: `Production`. Enabled/URL reflect config overrides present then, not just YAML defaults.

21 declared sources, 12 currently enabled.

## AwsIpRanges (`AwsIpRanges`)

- **Enabled**: yes
- **URL**: `https://ip-ranges.amazonaws.com/ip-ranges.json`
- **Purpose**: Official AWS IP ranges. Feeds datacenter/cloud-origin detection — without it, requests proxied through AWS infrastructure lose that signal entirely.
- **Licence**: Public data, no redistribution restriction (AWS-published reference doc)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## AzureIpRanges (`AzureIpRanges`)

- **Enabled**: no
- **URL**: _none_
- **Purpose**: Official Microsoft Azure IP ranges. Disabled by default — Microsoft rotates the download URL weekly (see microsoft.com/en-us/download/details.aspx?id=56519), so there is no stable default URL to ship; an operator must supply the current one.
- **Licence**: Public data, no redistribution restriction (Microsoft-published reference doc)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## Bot Pattern List (bucket) (`BotPatternsGroup`)

- **Enabled**: yes
- **URL**: _none_
- **Purpose**: Bucket-level last-update timestamp shared by the 3 sources it covers (IsBot, Matomo, CrawlerUserAgents) — list_updates has no per-source rows, only this bucket, so this is the true precision available today, not an invented per-source one.
- **Licence**: _not specified_
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase) — list_updates table
- **Live status tracked**: yes
- **Covers**: IsBot, Matomo, CrawlerUserAgents — this entry's timestamp is the only precision available for those sources today, not a per-source one.

## BrowserVersions (`BrowserVersions`)

- **Enabled**: yes
- **URL**: `https://www.browsers.fyi/api`
- **Purpose**: Latest major-browser versions for age checking. Bots often report outdated browser versions; without current version data, version-age detection falls back to compiled-in defaults that go stale over time.
- **Licence**: Public API, no stated redistribution restriction (browsers.fyi)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## CISA Known Exploited Vulnerabilities (`CisaKev`)

- **Enabled**: no
- **URL**: `https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json`
- **Purpose**: CISA Known Exploited Vulnerabilities catalog. Flags requests exploiting CVEs known to be actively used in the wild. Disabled by default (FOSS posture: everything under ThreatIntel is opt-in) — without it, CVE-based threat correlation doesn't run at all, not degraded, off.
- **Licence**: US government public data, no redistribution restriction
- **Cadence**: 1h, via ThreatIntelRefreshService
- **Failure mode**: FailOpen
- **On-disk location**: in-memory cache only (ThreatIntelCoordinator); no disk persistence
- **Live status tracked**: no — see the next major version's per-source rollout

## CloudflareIpv4 (`CloudflareIpv4`)

- **Enabled**: yes
- **URL**: `https://www.cloudflare.com/ips-v4`
- **Purpose**: Official Cloudflare IPv4 ranges. Identifies traffic proxied through Cloudflare so origin-vs-proxy signals stay accurate.
- **Licence**: Public data, no redistribution restriction (Cloudflare-published reference doc)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## CloudflareIpv6 (`CloudflareIpv6`)

- **Enabled**: yes
- **URL**: `https://www.cloudflare.com/ips-v6`
- **Purpose**: Official Cloudflare IPv6 ranges — IPv6 counterpart to CloudflareIpv4.
- **Licence**: Public data, no redistribution restriction (Cloudflare-published reference doc)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## Fastly Public IP List (`CloudRangesFastly`)

- **Enabled**: no
- **URL**: `https://api.fastly.com/public-ip-list`
- **Purpose**: Fastly's public IP list. The CloudRanges provider surfaces AWS/Azure/GCP/ Cloudflare too, but those come from BotListFetcher's own IP-range sources — Fastly isn't covered there, so this provider fetches it directly. Disabled by default (FOSS posture: everything under ThreatIntel is opt-in).
- **Licence**: Public data, no redistribution restriction (Fastly-published reference API)
- **Cadence**: 24h, via ThreatIntelRefreshService
- **Failure mode**: FailOpen
- **On-disk location**: in-memory cache only (ThreatIntelCoordinator); no disk persistence
- **Live status tracked**: no — see the next major version's per-source rollout

## CoreRuleSetScanners (`CoreRuleSetScanners`)

- **Enabled**: yes
- **URL**: `https://raw.githubusercontent.com/coreruleset/coreruleset/main/rules/scanners-user-agents.data`
- **Purpose**: OWASP CoreRuleSet scanner user-agent patterns (the same feed ModSecurity deployments use). Feeds SecurityToolDetector alongside ScannerUserAgents.
- **Licence**: Apache-2.0 (github.com/coreruleset/coreruleset)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## CrawlerUserAgents (`CrawlerUserAgents`)

- **Enabled**: no
- **URL**: `https://raw.githubusercontent.com/monperrus/crawler-user-agents/master/crawler-user-agents.json`
- **Purpose**: Community-maintained crawler list with source URLs. Disabled by default because IsBot already incorporates these patterns.
- **Licence**: MIT (github.com/monperrus/crawler-user-agents)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## Datacenter IP Ranges (bucket) (`DatacenterIpsGroup`)

- **Enabled**: yes
- **URL**: _none_
- **Purpose**: Bucket-level last-update timestamp shared by the 5 sources it covers (AwsIpRanges, GcpIpRanges, AzureIpRanges, CloudflareIpv4, CloudflareIpv6) — list_updates has no per-source rows, only this bucket, so this is the true precision available today, not an invented per-source one.
- **Licence**: _not specified_
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase) — list_updates table
- **Live status tracked**: yes
- **Covers**: AwsIpRanges, GcpIpRanges, AzureIpRanges, CloudflareIpv4, CloudflareIpv6 — this entry's timestamp is the only precision available for those sources today, not a per-source one.

## GcpIpRanges (`GcpIpRanges`)

- **Enabled**: yes
- **URL**: `https://www.gstatic.com/ipranges/cloud.json`
- **Purpose**: Official Google Cloud IP ranges. Feeds datacenter/cloud-origin detection for GCP-hosted traffic.
- **Licence**: Public data, no redistribution restriction (Google-published reference doc)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## IsBot (`IsBot`)

- **Enabled**: yes
- **URL**: `https://raw.githubusercontent.com/omrilotan/isbot/main/src/patterns.json`
- **Purpose**: Most comprehensive bot-pattern source; aggregates crawler-user-agents, matomo, myip.ms and manual curated additions. Primary UA regex source — most bot-signature matches come from this feed. Without it, UA-pattern matching falls back to the much smaller embedded BotSignatures constants.
- **Licence**: MIT (github.com/omrilotan/isbot)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## Matomo (`Matomo`)

- **Enabled**: no
- **URL**: `https://raw.githubusercontent.com/matomo-org/device-detector/master/regexes/bots.yml`
- **Purpose**: Categorized bot patterns with metadata (name, category, url). Disabled by default because IsBot already incorporates these patterns; enable only if per-bot category info is needed downstream.
- **Licence**: MIT (github.com/matomo-org/device-detector)
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## Web-Bot-Auth Public Key Registry (`PublicKeyRegistry`)

- **Enabled**: no
- **URL**: _none_
- **Purpose**: Web-Bot-Auth (RFC 9421 signature) public-key manifest, e.g. Cloudflare's AI-agent registry. No default URL — opt-in; manual keys always work regardless of remote fetch.
- **Licence**: Depends on configured manifest source; none shipped by default
- **Cadence**: Tick1h, 01:00:00 since last success (floor 1h)
- **Failure mode**: FailOpen
- **On-disk location**: in-memory only (no snapshot path configured)
- **Live status tracked**: no — see the next major version's per-source rollout

## ScannerUserAgents (`ScannerUserAgents`)

- **Enabled**: yes
- **URL**: `https://raw.githubusercontent.com/digininja/scanner_user_agents/main/list.json`
- **Purpose**: Security scanner user agents (Nikto, Nessus, SQLMap, WPScan, etc.) with tool metadata. Feeds SecurityToolDetector for the API-honeypot security layer.
- **Licence**: See github.com/digininja/scanner_user_agents for terms
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (SQLite, via BotListDatabase)
- **Live status tracked**: no — see the next major version's per-source rollout

## Spamhaus DROP/EDROP (`SpamhausDrop`)

- **Enabled**: no
- **URL**: `https://www.spamhaus.org/drop/drop.txt`
- **Purpose**: Spamhaus DROP + EDROP: netblocks hijacked or leased by professional spam/ cybercrime operations. secondary_url is EDROP (extended DROP); set empty to skip it. Disabled by default (FOSS posture: everything under ThreatIntel is opt-in).
- **Licence**: Spamhaus DROP/EDROP free for use, see spamhaus.org/drop for terms
- **Cadence**: 12h, via ThreatIntelRefreshService
- **Failure mode**: FailOpen
- **On-disk location**: in-memory cache only (ThreatIntelCoordinator); no disk persistence
- **Live status tracked**: no — see the next major version's per-source rollout

## TLS/JA3 Reference Corpus (`TlsCorpus`)

- **Enabled**: no
- **URL**: _none_
- **Purpose**: Signed JA3 fingerprint reference corpus for TLS-based bot detection. No default URL — opt-in, operator must supply a signed envelope source. Embedded baseline covers cold start regardless.
- **Licence**: Operator-supplied (signed envelope from the vendor pipeline); not a third-party feed
- **Cadence**: Tick1h, 06:00:00 since last success (floor 5min)
- **Failure mode**: FailOpen
- **On-disk location**: in-memory only (IJa3ReferenceIndex); embedded baseline seeds cold start
- **Live status tracked**: no — see the next major version's per-source rollout

## Tor Exit Node List (`TorExit`)

- **Enabled**: no
- **URL**: `https://check.torproject.org/torbulkexitlist`
- **Purpose**: Tor exit-node IP list. Flags traffic exiting the Tor network. Disabled by default (FOSS posture: everything under ThreatIntel is opt-in).
- **Licence**: Tor Project public data, no redistribution restriction
- **Cadence**: 30min, via ThreatIntelRefreshService
- **Failure mode**: FailOpen
- **On-disk location**: in-memory cache only (ThreatIntelCoordinator); no disk persistence
- **Live status tracked**: no — see the next major version's per-source rollout

## VpnAsns (`VpnAsns`)

- **Enabled**: yes
- **URL**: `https://raw.githubusercontent.com/tn3w/IPSet/master/datacenter_asns.json`
- **Purpose**: Consumer VPN / datacenter ASN list (1.7M+ IPs; ExpressVPN, Surfshark, ProtonVPN, PIA, CyberGhost, Mullvad + datacenter ASNs). Feeds IpAtom's ip.is_vpn signal. The YAML vpn_egress_asns seeds in ip.detector.yaml remain the offline fallback when this feed is unreachable.
- **Licence**: Free/open dataset (github.com/tn3w/IPSet) — see repo for terms
- **Cadence**: BotListUpdateService, Tick1h subscription, 24h since last success, floor 60m checks
- **Failure mode**: FailOpen
- **On-disk location**: Bot list: botdetection.db (falls back to YAML seeds in ip.detector.yaml)
- **Live status tracked**: no — see the next major version's per-source rollout

## Arcjet Well-Known Bots (`WellKnownBots`)

- **Enabled**: yes
- **URL**: `https://raw.githubusercontent.com/arcjet/well-known-bots/main/well-known-bots.json`
- **Purpose**: Structured catalog of known bot identities (name, category, operator, verification method) used to name and classify detections beyond raw UA regex matching. Feeds WellKnownBotIndex and seeds identity archetypes. Has an embedded baseline (WellKnownBotBaseline) so classification still works cold-start or air-gapped — losing this feed means the catalog goes stale, not empty.
- **Licence**: See github.com/arcjet/well-known-bots for terms
- **Cadence**: Tick1h, 1.00:00:00 since last success (floor 1h)
- **Failure mode**: FailOpen
- **On-disk location**: in-memory only (WellKnownBotIndex); embedded baseline seeds cold start
- **Live status tracked**: no — see the next major version's per-source rollout

